# todo

 - [ ] allow clearing of the task
 - [ ] in the list of entries highlight the in-progress one
 - [x] when starting a new entry ask for the task up front
 - [x] if updating the in-progress entry don't prompt for logging
 - [x] don't ask to log entries that don't have tasks
 - [ ] make the list of tasks be part of the menu itself? Then can use arrow keys or hotkeys to select certain pieces or functionalities?


 for the idea to make the entry table part of the menu with selectable entries, the selectionprompt would have both the entries and other options (like logging a task group).. perhaps use a tree with nested prompts?

 OR can the selectionprompt options be styled to look like rows in a table?